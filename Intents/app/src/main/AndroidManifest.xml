<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.intents">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.IntentsExample">
        <activity
            android:name="com.example.intents.MainActivity"
            android:exported="true"
            android:label="Explicit Intent Example">
        </activity>

        <activity
            android:name="com.example.intents.IntentActivity"
            android:exported="true"></activity>

        <activity
            android:name="com.example.intents.ImplicitManagerActivity"
            android:exported="true"
            android:label="Explicit Activities">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

    <uses-permission android:name="android.permission.CALL_PHONE" />
</manifest>